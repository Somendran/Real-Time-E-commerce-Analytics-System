# Copy this file to .env and replace with your actual connection string.
# Preferred variable name:
SUPABASE_DB_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Optional fallback (used if SUPABASE_DB_URL is not set):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
